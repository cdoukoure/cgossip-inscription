/* import { StateModule } from './root-store.module';

describe('StoreModule', () => {
  let storeModule: StateModule;

  beforeEach(() => {
    storeModule = new StateModule();
  });

  it('should create an instance', () => {
    expect(storeModule).toBeTruthy();
  });
});
 */